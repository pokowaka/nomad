# This is an example secrets file.
# Copy this to 'secrets/nomad.env' and fill in your actual credential.
# DO NOT COMMIT the 'nomad.env' file to your repository.

#
# WireGuard Peer 1 (e.g., Work VPN)
#
WG0_PRIVATE_KEY="<REPLACE_ME_WORK_PRIVATE_KEY>"
WG0_PUBLIC_KEY="<REPLACE_ME_WORK_PEER_PUBLIC_KEY>"
WG0_ENDPOINT="<REPLACE_ME_WORK_ENDPOINT_IP>:<REPLACE_ME_WORK_ENDPOINT_PORT>"
WG0_ADDRESS="10.10.10.2/32" # Example Client IP for the router on this VPN

#
# WireGuard Peer 2 (e.g., Home VPN)
#
WG1_PRIVATE_KEY="<REPLACE_ME_HOME_PRIVATE_KEY>"
WG1_PUBLIC_KEY="<REPLACE_ME_HOME_PEER_PUBLIC_KEY>"
WG1_ENDPOINT="<REPLACE_ME_HOME_ENDPOINT_IP>:<REPLACE_ME_HOME_ENDPOINT_PORT>"
WG1_ADDRESS="10.20.20.2/32" # Example Client IP for the router on this VPN

# Allowed IPs for the peers. 0.0.0.0/0 routes all traffic through the VPN.
WG0_ALLOWED_IPS="0.0.0.0/0"
WG1_ALLOWED_IPS="0.0.0.0/0"
