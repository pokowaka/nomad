# nomad
Deploy the Cudy TR3000 as a hardened, low-profile travel gateway. The device prioritizes friction-free "Headless Operation" for the user while enforcing a zero-trust security posture against untrusted ISPs through TTL normalization and strictly encrypted DNS.
